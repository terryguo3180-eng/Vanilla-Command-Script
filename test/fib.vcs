main() -> Int:
    result: Int = 0

    for i: Int = 1; i <= 24; i += 1:
        result = fib(i)
        tell result

    return result

fib(n: Int, m: Int = 0) -> Int:
    if m > 0:
        n -= m
    if n == 1 or n == 2:
        return 1
    return fib(n, 1) + fib(n, 2)
